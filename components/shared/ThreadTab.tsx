interface Props{
    currentUserId: string;
    accountId: string;
    accountType: string
}

const ThreadTab = ({currentUserId,accountId, accountType}:Props) => {
  return (
    <div>
      
    </div>
  )
}

export default ThreadTab
